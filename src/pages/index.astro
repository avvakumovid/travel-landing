---
import { getCollection } from "astro:content";
import ScreenLayout from "../layouts/ScreenLayout.astro";
const allScreens = await getCollection("screens");
---

<>
  {allScreens.map((screen) => <ScreenLayout {...screen.data} />)}
</>
